---
title: Development Environment
description: Setting up a productive VirtEngine development environment — direnv, pnpm frontend workspaces, and the Docker-based localnet.
---

This page covers the tooling around the build: environment management,
frontend workspaces, and running a complete local network.

## direnv

Both the `virtengine` and provider repositories use
[direnv](https://direnv.net) extensively to set up and update the
environment as you move through the source tree. Minimum required version
is **2.32.x** — some distributions ship older builds; use the binary
releases if needed.

Whitelist your checkouts to avoid constant `direnv allow` prompts, in
`${XDG_CONFIG_HOME:-$HOME/.config}/direnv/direnv.toml`:

```toml
[whitelist]
prefix = [
    "<path to virtengine sources>",
    "<path to provider-services sources>"
]
```

> **Note:** The `make cache` and `make cache-clean` targets (defined in
> `make/setup-cache.mk:9,76`) require the `VE_DEVCACHE` environment
> variable, which is supplied by direnv via `.envrc`. Without direnv
> (or manually exporting the `VE_*` variables), these targets will fail
> or behave unexpectedly. On Windows, the Makefile skips the direnv check
> and falls back to `VE_ROOT=$(CURDIR)` — see `make/init.mk:18-38`.

## Frontend tooling (pnpm)

VirtEngine uses **pnpm** for frontend workspaces:

```shell
corepack enable
corepack prepare pnpm@10.28.2 --activate
pnpm --version
```

Install workspace dependencies when touching frontend code:

```shell
pnpm -C portal install    # Portal app
pnpm -C sdk/ts install    # TypeScript SDK
```

## Localnet

The localnet provides a complete local development environment with all
VirtEngine services running in Docker containers.

**Prerequisites:** Docker and Docker Compose, Go 1.26.8+ (matching
`go.mod`), and a Bash shell (WSL2 on Windows; native on Linux/macOS).

```bash
# First time only
chmod +x scripts/localnet.sh scripts/init-chain.sh

# Start everything
./scripts/localnet.sh
```

The localnet gives you a running chain to develop against without touching
a public network — the fastest loop for module, daemon, and client work.

## Repository layout orientation

| Path | Contents |
| --- | --- |
| `x/` | The 27 on-chain modules — see the [module map](/protocol/modules/) |
| `pkg/` | Shared Go packages (economics simulation, HPC workload library, provider daemon internals) |
| `cmd/` | Binaries, including `hpc-node-agent` |
| `sdk/ts/` | TypeScript SDK workspace |
| `portal/` | Portal frontend workspace |
| `mobile/veid-capture-app/` | VEID capture reference app |
| `docs/`, `_docs/` | In-repo technical and operational documentation |
| `make/` | Build system internals (`init.mk`, `setup-cache.mk`) |
| `config/mainnet/` | Mainnet launch configuration and genesis bundle |

## Testing

Go tests run per package as usual; the Makefile wires broader gates. Two
useful entry points:

```bash
# Economics simulation & audit framework
go test -v ./pkg/economics/...

# A single module
go test ./x/settlement/...
```
