{
  "name": "identity-org-au",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "identity.org.au — the Identity Wallet for the VirtEngine ecosystem, powered by VEID",
  "scripts": {
    "dev": "astro dev",
    "build": "node ./scripts/og.mjs && astro build",
    "check:links": "node ../../scripts/check-links.mjs dist",
    "check:types": "astro check",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.3.0",
    "@fontsource/public-sans": "^5.2.0",
    "@tailwindcss/vite": "^4.1.0",
    "astro": "^5.6.0",
    "sharp": "^0.33.5",
    "tailwindcss": "^4.1.0"
  },
  "engines": {
    "node": ">=20.3.0"
  },
  "//typescript": "Pinned to ^6 ON PURPOSE. `astro check` (the check:types gate) needs the TypeScript 6.x programmatic API: a plain `pnpm add -D typescript` resolves 7.x and `astro check` then hard-fails with \"The TypeScript module loaded (found 7.0.2) does not expose the programmatic API that astro check relies on\". Do not bump without re-running `pnpm run check:types` in this site.",
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
