---
/**
 * BuyPaths — the acquisition-path instrument, shared by the homepage and the
 * Waldur page so both teach the same three paths: direct order (default),
 * open bid, and attribute selector. Zero-JS radio tabs; styling lives in the
 * `.buy-ex` rules in global.css.
 */
import { ACQUISITION_PATHS } from "@data/market";

interface Props {
  foot?: string;
  class?: string;
}

const { foot, class: className } = Astro.props;
---

<div class:list={["buy-ex", className]}>
  {
    ACQUISITION_PATHS.map((p) => (
      <input type="radio" name="buy-mode" id={`buy-${p.id}`} checked={p.id === "direct"} />
    ))
  }
  <div class="buy-tabs" role="tablist" aria-label="Acquisition paths">
    {
      ACQUISITION_PATHS.map((p) => (
        <label class="buy-tab" for={`buy-${p.id}`}>
          <small>{p.n}</small>
          <span>{p.tab}</span>
        </label>
      ))
    }
  </div>
  <div class="buy-stage">
    {
      ACQUISITION_PATHS.map((p) => (
        <article class={`buy-panel buy-panel--${p.id}`} data-for={`buy-${p.id}`}>
          <div class="buy-copy">
            <p class="eyebrow">{p.tag}</p>
            <h3 class="mt-4 text-xl font-semibold">{p.title}</h3>
            <p class="mt-3 text-sm leading-relaxed text-slate">{p.body}</p>
            <ol class="buy-steps">
              {p.steps.map(([k, v], i) => (
                <li>
                  <span class="buy-step-n" aria-hidden="true">{String(i + 1).padStart(2, "0")}</span>
                  <span><strong>{k}</strong><small>{v}</small></span>
                </li>
              ))}
            </ol>
            <p class="buy-when"><strong>When to use it — </strong>{p.when}</p>
          </div>
          <div class="buy-art" aria-hidden="true">
            {p.id === "direct" && (
              <div class="buy-flow">
                <div class="buy-chip buy-chip--offer"><small>Catalogue</small><strong>Harbour GPU</strong><span>$0.39 / GPU-hr</span></div>
                <i class="buy-arrow">→</i>
                <div class="buy-chip"><small>Order</small><strong>names that listing</strong><span>escrow funded</span></div>
                <i class="buy-arrow">→</i>
                <div class="buy-chip buy-chip--win"><small>Lease</small><strong>matched at list</strong><span>0 bids needed</span></div>
              </div>
            )}
            {p.id === "bid" && (
              <div class="buy-bids">
                <div class="buy-chip"><small>Open order</small><strong>8× GPU · ap-southeast</strong><span>max $0.45 · window open</span></div>
                <ul>
                  <li><span>Provider A</span><b>$0.42</b></li>
                  <li class="buy-bid--win"><span>Provider B</span><b>$0.39 · accepted</b></li>
                  <li><span>Provider C</span><b>$0.51</b></li>
                </ul>
                <div class="buy-chip buy-chip--win"><small>Lease</small><strong>Provider B · $0.39</strong><span>escrow funded</span></div>
              </div>
            )}
            {p.id === "selector" && (
              <div class="buy-flow">
                <div class="buy-attrs"><span>gpu</span><span>ap-southeast</span><span>≤ $0.45</span><span>attested</span></div>
                <i class="buy-arrow">→</i>
                <div class="buy-chip buy-chip--win"><small>Matched listing</small><strong>Harbour GPU</strong><span>eligible · capped</span></div>
              </div>
            )}
          </div>
        </article>
      ))
    }
  </div>
  {foot && <p class="buy-foot">{foot}</p>}
</div>
